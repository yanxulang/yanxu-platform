引「包:言台」为 平台；
引「标准:测试」为 测试；
引「标准:字节」为 字节；

法 忽略事件（批次）则 终

定 协议：典 为 平台.协议查询（）；
测试.相等（协议【「ABI」】，2）；
测试.相等（协议【「平台主」】，1）；
测试.相等（协议【「绘制主」】，1）；

定 应用 为 平台.应用（「言台包装测试」，忽略事件）；
定 窗口 为 应用.窗口（{「标题」：「测试窗口」，「宽」：320，「高」：200，「可见」：假}）；
测试.相等（窗口.查询（）【「标题」】，「测试窗口」）；
测试.相等（窗口.查询（）【「宽」】，320）；

定 缓冲：字节串 为 平台.绘制编码（【
    {「类型」：「清空」，「颜色」：【250，250，250，255】}，
    {「类型」：「填充矩形」，「矩形」：【10，10，80，30】，「颜色」：【30，100，220，255】}，
    {「类型」：「文字」，「位置」：【12，16】，「最大宽」：200，「字号」：16，「行高」：20，「颜色」：【0，0，0，255】，「文本」：「言台」}
】）；
测试.断言（字节.长度（缓冲）大于 16，「绘制编码必须产生二进制帧」）；
测试.相等（长度（平台.绘制检查（缓冲）【「操作码」】），3）；
测试.相等（窗口.提交缓冲（缓冲），3）；

定 计时器 为 应用.计时器（50，假）；
测试.相等（计时器.查询（）【「重复」】，假）；
计时器.取消（）；
测试.相等（计时器.查询（）【「已取消」】，真）；

定 测量：典 为 应用.文字测量（「中文 IME」，{「字号」：16，「行高」：20，「最大宽」：400}）；
测试.断言（测量【「宽」】大于 0，「中文文字必须可测量」）；
测试.断言（测量【「基线」】大于 0，「文字测量必须返回基线」）；

应用.关闭（）；
言「平台包装集成测试通过」；
