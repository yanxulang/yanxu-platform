# 绘制协议畸形语料

`draw-v1-malformed.tsv` 是绘制协议 v1 解码器的可复用回归语料。每个非注释行包含三个
制表符分隔字段：用例名、预期错误类别和十六进制输入；`-` 表示空输入。

语料只记录稳定错误类别，不依赖中文错误文案。新增协议字段或修复解析缺陷时，应把最小
复现输入加入语料，并保证旧语料仍保持原有分类。集成测试还会对一个合法帧执行全部前缀
截断和逐字节边界变异，以证明解码器始终返回有界结果而不发生恐慌。
